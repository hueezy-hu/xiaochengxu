# 泰斓 V1.7 第三阶段审计与缺口清单

> 审计日期：2026-07-13
> 代码基线：`6f44aac`（main）+ 本轮 phase3 本地准备提交
> 原则：本地通过 ≠ 云端已完成；不得声称正式上线。

## 1. 第一阶段审计结果（已执行）

### Git

| 项 | 结果 |
|---|---|
| 分支 | `main` |
| HEAD | `6f44aac`（V1.7 全量验收基线） |
| 工作树 | 干净；仅未跟踪 `open-design/`（按要求不改不删） |
| 相对 origin | 本地领先 16 个提交，未 push |

### 自动验收（真实输出）

```
node scripts/check-integrity.js
→ INTEGRITY PASS (141→145 files, 48 frontend actions)

node --test cloudfunctions/api/tests/*.test.js scripts/tests/*.test.js
→ tests pass（基线 56；phase3 准备后 62），fail 0
```

### 源码扫描

- JS / JSON / WXML 完整性：由 integrity 脚本覆盖
- NUL 字节：0
- UTF-8 源文件解析：通过
- 前后端 action 交叉：48 frontend actions 对齐

### 微信开发者工具

- `project.config.json` 含 `appid=wxb64f1ee0f8167f14`，`miniprogramRoot`/`cloudfunctionRoot` 正确
- 本机 CLI 未在 PATH，**未能在本环境自动编译**；需你在开发者工具中打开目录验证导入与编译
- `app.json` 未声明 `"cloud": true`（依赖 `app.js` 的 `wx.cloud.init`）；若工具提示云开发未开启，再确认云环境绑定

## 2. 外部配置状态

| 配置项 | 代码侧 | 云端/运营侧 | 阻塞正式支付上线 |
|---|---|---|---|
| 小程序 AppID | 已有 `wxb64f1ee0f8167f14` | 需确认主体与权限 | 否（已配置） |
| 云开发环境 ID | 使用 `DYNAMIC_CURRENT_ENV` | **需你确认实际 envId 与权限** | 部署需要 |
| `SUPER_ADMIN_OPENID` | 读 `process.env` | **未在本机验证云端是否已设** | 管理端需要 |
| 微信支付商户号/证书/APIv3 | **无** | **你需提供** | 是 |
| 成团结果模板 ID | `config.groupResultTemplateId` 入口已有，initDemo 为空 | **你需申请并填写** | 正式订阅需要 |
| 取货提醒模板 ID | `config.pickupTemplateId` 入口已有，initDemo 为空 | **你需申请并填写** | 正式订阅需要 |
| 数据库备份 | 文档要求有 | **未执行（禁止擅自操作云库）** | 部署前必须 |

## 3. 代码事实 vs 文档（重要纠偏）

| 声明 | 事实 |
|---|---|
| CLAUDE 曾写「真实支付代码路径已写好」 | **不准确**。`payOrder` 在 `mockPay=false` 时直接返回「真实支付尚未接入」；`payCallback` 固定失败桩 |
| 退款真实路径 | `retryRefunds` 有 `cloud.cloudPay.refund` 调用骨架，**未做回调验签/对账/联调** |
| 订阅消息 | 前端可拉授权；outbox **只写不发**（本轮已补发送器骨架，无模板 ID 时标记「跳过-无模板」，不伪造成功） |
| V1.7 业务 | 购物车、3 分钟预占、按人成团、双时点、两段退款、核销/放置/照片等 **本地演示完整** |

## 4. 第三阶段还缺什么

### 必须等你提供资料

1. 微信支付：商户号、证书、APIv3 Key、云开发支付配置
2. 两个订阅消息模板 ID（字段名须与发送 payload 对齐）
3. 云环境 envId + 部署权限确认
4. `SUPER_ADMIN_OPENID` 真实 openid
5. **明确确认**后才能：备份库、部署云函数、上传小程序、清库/initDemo

### 不依赖商户资料、可本地继续（本轮已做部分）

1. ~~MOCK_PAY 环境变量开关（默认 true）~~ 已做
2. ~~time_expire / 回调分类纯函数与测试~~ 已做
3. ~~notificationOutbox 发送器 + 无模板跳过 + 授权校验 + 失败重试计数~~ 已做
4. 真实 `cloudPay.unifiedOrder` / 回调验签落地（**仍需商户资料**）
5. 迟到支付成功：库存重确认或自动退款全链路
6. 真实退款回调 + 查询 + 对账
7. 自提提醒按窗口开始时间入队（当前主要有成团结果 notice）
8. 迁移方案执行与同版部署
9. 真机冒烟全清单

## 5. 可立即执行 vs 必须等待

### 可立即执行（不改正式云数据）

- 本地测试与 integrity
- 代码层支付/订阅骨架与单元测试
- 开发者工具打开项目做编译与 MOCK 流程点验
- 填写外部配置清单（由你在微信后台申请）

### 必须等待你的授权 + 资料

- `MOCK_PAY=false`
- 云函数上传部署
- 数据库备份/迁移/清空/initDemo
- 小程序上传与提审
- 真实支付/退款联调
- 订阅消息真实发送验证

## 6. 部署与回滚（预备）

- **同版原则**：小程序 + `cloudfunctions/api` 必须同一 Git SHA
- **发布前**：工作树干净、备份业务集合、记录 SHA
- **回滚**：停发布 → 恢复数据库备份 → 部署上一稳定云函数与小程序；不在线上热改
- **禁止**：正式环境 `initDemo` 覆盖真实数据；先后端后改前端的混版

## 7. 本轮本地改动摘要

- `src/shared/runtime-config.js`：`MOCK_PAY` / `MANUAL_PHONE` 环境解析，默认 true
- `src/services/payment-helpers.js`：`time_expire` 对齐 3 分钟、回调分类（含 late_success）
- `src/services/notification-outbox.js`：outbox 处理、无模板跳过、授权跳过、失败不计成功
- `index.js`：接入环境开关；`lifecycleTick` 后处理 outbox
- 测试：`v17-phase3-prep.test.js`（6 项）
