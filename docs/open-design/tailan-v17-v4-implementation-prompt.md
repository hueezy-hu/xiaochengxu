# Open Design 任务：完成泰斓 V1.7 高保真增量设计稿

请直接执行，不再询问问题。合理细节按现有 v3 组件和 PRD V1.7 决定。

## 读取顺序

1. `AGENTS.md` 与 `CLAUDE.md`
2. `TAILAN-DESIGN.md`
3. `设计稿v3-解包版.html`
4. `banlan-cake-prd-v1.7.md`
5. `cloudfunctions/api/ACTIONS.md`
6. `docs/superpowers/specs/2026-07-13-tailan-v17-open-design-incremental-design.md`
7. `scripts/tests/v17-design-prototype.test.js`

## 目标文件

只修改 `设计稿v4-V1.7.html`。绝对禁止修改 `设计稿v3-解包版.html`，也不要修改小程序源码。

v4 已由 v3 复制而来。请在完全复用 `TAILAN-DESIGN.md` 和 v3 组件的前提下，做增量改稿，不要重新设计。

## 最终页面编号

必须形成 0～15 共 16 个手机框，每屏 390×844，横向排列：

0. 首页：开团中／今日休息／未开团三态，可点击切换；未开团含“催开团”和已催人数。
1. 商品分类：满 5 人成团；右上悬浮购物车和数量角标。
2. 商品详情：底部“加入购物车＋立即购买”。
3. 购物车（标题标注“V1.7 新增”）：逐项勾选、全选、数量增减、失效商品置灰沉底、勾选合计、去结算、加购不占库存。
4. 选择自提站点：已拼 X/5 人；每站显示“有人核销”或“无人放置”。
5. 结算及手机号（标题标注“V1.7 新增”）：联系人、手机号、尾号提示、站点、商品、发起支付。
6. 发起支付及预占（标题标注“V1.7 新增”）：3:00 倒计时；后台预占、无待支付列表、取消或超时需重新发起；成功／取消／超时三态可点击切换。
7. 支付成功：已拼 X/5 人；成团结果通知、取货提醒两个授权项。
8. 站点团页：已拼 X/5 人；分享落地和我也要买。
9. 我的订单：待自提／已完成／已退款／已完成未取；交付前“整单退款”，交付后“申请退款”。
10. 订单详情及取货券：手机尾号后 4 位大字凭证、二维码、交付模式、交付现场照片、两段式退款。
11. 我的：头像昵称、我的订单、联系商家、手机号管理、使用规则、超管“商家工作台”入口、核销员“现场核销”入口。
12. 商家工作台：今日休息开关、催开团计数、各站已拼 X/5 人、低于 5 人手动确认配送、成团后手动取消配送、待处理退款申请。
13. 商品管理：改价即时生效；有订单引用只能下架不能删。
14. 批次发布：勾 SKU、填共享库存、勾启用站点；站点固定资料不重复填；10:00 软提醒。
15. 现场核销／无人放置：扫码、输手机尾号、同场次尾号重复预警、交付拍照、整场已放置。

## 视觉硬约束

- 严格使用 `TAILAN-DESIGN.md`，禁止增加新色值、新字体、新圆角体系、新卡片风格。
- 继续使用 v3 的手机框、外部编号标题、内联 SVG、卡片和胶囊组件。
- C 端保持明亮；12～15 商家端保持深色。
- 图片仅用现有占位方式，不调用外部图片生成。
- 首页、支付和状态演示使用原生 JavaScript 切换，离线打开可操作。

## 业务硬约束

- 成团只能按“人”：统一示例“已拼 4/5 人”，不得出现按份成团。
- 库存仍按“份/个”，必须与拼团人数明确区分。
- 预占固定 3 分钟，无用户待支付列表。
- 核销凭证固定为手机号尾号后 4 位，保留二维码。
- 不支持顺延或跨批次迁移。
- 22:00 只锁定成团结果；取货日 12:00 才执行配送确认或关闭退款。
- 交付前自助整单即时退款；交付后申请人工退款。

## 交互与交付

- 顶部快速导航必须覆盖 `#home`、`#p1`～`#p15`。
- 主线必须可点击：首页→分类→详情→购物车／立即购买→选站→结算→支付预占→支付成功→我的订单→取货券。
- 不允许重复 id，不允许内部锚点指向不存在的目标。
- 页面头部或尾部增加“相对 v3 修改清单／V1.7 新增清单”。
- 删除旧 UI 文案：15 分钟、6 位码、满 5 件／满 4 份成团、10:00 后不可退款、顺延下一批。

## 自检

完成后运行：

```powershell
node --test scripts/tests/v17-design-prototype.test.js
```

若失败，继续修复到通过。再检查 v4 无 NUL 字节，结尾完整。最终回复总结完成的 16 屏、交互状态和测试结果。
