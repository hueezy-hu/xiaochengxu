# v3变更自查表

## R1 日批次模型：今天拼·明天取
- 代码位置：`cloudfunctions/api/domain.js:7` `beijingTime()`；`cloudfunctions/api/domain.js:28` `beijingTimestamp()`；`cloudfunctions/api/index.js:88` `currentSaleDates()`；`cloudfunctions/api/index.js:160` `initDemo()`；`cloudfunctions/api/index.js:594` `createBatch()`。
- 页面呈现：`miniprogram/pages/home/home.wxml:34` 首页“今天拼 · 明天取”；`miniprogram/pages/catalog/catalog.js:59` 分类页批次状态条；`miniprogram/pages/product/product.wxml:15` 商品详情规则；`miniprogram/pages/adminBatch/adminBatch.wxml` 创建批次取货日与截止时间表单。

## R2 成团门槛默认 4 份
- 代码位置：`cloudfunctions/api/index.js:160` `initDemo()` 写入三站 `thresholdN: 4`；`cloudfunctions/api/index.js:594` `createBatch()` 默认 `thresholdN || 4`；`cloudfunctions/api/domain.js:205` `advanceBatchLifecycle()` 按 `thresholdN` 和 `paidItemCount` 判断成团。
- 页面呈现：`miniprogram/pages/home/home.wxml:47` 首页“满 4 份成团”；`miniprogram/pages/catalog/catalog.wxml:7` 分类页状态条；`miniprogram/pages/product/product.wxml:15` 商品详情规则；`miniprogram/pages/pickStation/pickStation.wxml` 站点卡 `已拼 paid/threshold`。

## R3 取货日 10:00 前自助退款
- 代码位置：`cloudfunctions/api/domain.js:142` `canSelfCancelOrder()`；`cloudfunctions/api/domain.js:161` `applyRefundToSnapshots()`；`cloudfunctions/api/index.js:384` `cancelOrder()`；`cloudfunctions/api/tests/domain.test.js` 新增 9:59 可退、10:01 拒退、成团后退款回退库存和不取消成团用例。
- 页面呈现：`miniprogram/pages/home/home.wxml:48` 首页规则；`miniprogram/pages/product/product.wxml:30` 商品详情退款/转让说明；`miniprogram/pages/orders/orders.wxml` 订单列表自助退款按钮；`miniprogram/pages/orderDetail/orderDetail.wxml` 取货券自助退款按钮；`miniprogram/pages/mine/mine.js:88` 我的页规则弹窗。

## R4 自提窗口前置与状态机调整
- 代码位置：`cloudfunctions/api/index.js:160` `initDemo()` 创建三站 `deliveryWindows`；`cloudfunctions/api/index.js:594` `createBatch()` 按站点 `window` 写入 `deliveryWindows`；`cloudfunctions/api/index.js:645` `setDeliveryWindow()` 降级为修改窗口；`cloudfunctions/api/domain.js:205` `advanceBatchLifecycle()` 删除旧排期状态并推进成团站点到待自提；`cloudfunctions/api/index.js:747` `advanceBatchById()`；`cloudfunctions/api/index.js:771` `closeExpired()`。
- 页面呈现：`miniprogram/pages/pickStation/pickStation.wxml:18` 选站点展示窗口和取货点；`miniprogram/pages/orderDetail/orderDetail.wxml:11` 取货券展示窗口和“过时未取联系商家”；`miniprogram/pages/orderDetail/orderDetail.wxml:13` 取货点指引；`miniprogram/pages/adminBatch/adminBatch.wxml` 创建批次按站点配置窗口与指引。